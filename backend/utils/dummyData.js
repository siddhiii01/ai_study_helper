export function generateDummyTasks(topic) {
  return [
    `Explain "${topic}" in your own words`,
    `Build a small practical example using "${topic}"`,
    `Write one real-world use case where "${topic}" is applied`
  ];
}